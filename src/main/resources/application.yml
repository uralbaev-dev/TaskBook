server:
  port: 8080

spring:
  application:
    name: Task
  datasource:
    username: 'postgres'
    url: jdbc:postgresql://localhost:5432/task-book
    password: 'root123'
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    hibernate:
      ddl-auto: update
    show-sql: 'true'